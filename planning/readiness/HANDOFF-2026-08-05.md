# Handoff — 2026-08-05, 15:30Z

Para quem assume daqui. **Há um run de 12 h em andamento**; a primeira seção é a
única que você precisa ler antes de fazer qualquer coisa.

---

## 0. O que está acontecendo AGORA

**O R2-WP04 Final 1 está rodando no host de campanha.** Começou às
`2026-08-05T10:28:37Z`, dura 12 h, termina por volta das **22:28Z**.

```bash
ssh -i ~/Downloads/colossus.pem ubuntu@98.92.141.100 \
  'grep "^###" ~/final1.log; ls ~/soak-runs/soak/telemetry/stats-*.json | wc -l'
```

Progresso é a contagem de amostras contra **~43.235** (uma por segundo).

### ⚠️ Não rode carga no host enquanto isso

Qualquer coisa pesada em `98.92.141.100` contamina a medição. Trabalho local é
livre — **mas não rode `build/check.sh` em paralelo com outra coisa pesada na sua
máquina**: isso produziu dois vermelhos falsos nesta sessão, ambos em fixtures
que passam sozinhos.

---

## 1. Onde o programa está

| | Estado |
|---|---|
| **Portão** | **R1** — piloto interno não crítico. Nada nesta sessão o moveu |
| R2-WP01/02/03 | fechados |
| **R2-WP04** | escada pré-final **PASS inteira**; **Final 1 em curso**, Final 2 pendente |
| **R2-WP05** | três experimentos completos (`evidence/2026-08-04-r2-wp05-burst`, `2026-08-05-r2-wp05-lanes`, `2026-08-05-r2-wp05-knee`) |
| R2-WP06 | **fechado** em 2026-08-04 |
| R2-WP07 | fechado, risco aceito assinado |
| **R2-WP08** | **não iniciado** — depende dos dois finais |
| **L6** (R3) | **FECHADA** — `v0.11.0` publicada |
| Ambição | **A2 ratificada** (ADR-052): framework para outras pessoas usarem |

## 2. A história que você precisa entender antes de mexer em qualquer coisa

O R2-WP04 **parou** em 2026-08-04. Ele desceu a taxa três vezes fugindo de
recusas de saturação e o piso não se movia.

O R2-WP05 descobriu por quê, em três experimentos:

1. **A recusa é função da rajada de reconexão, não da taxa** — descer a taxa
   nunca ia convergir.
2. **Sobreassinar `max_handlers` não custa nada** — e melhora a cauda.
3. **Não há knee até 10.000 req/s**, e o "teto" de 2.350/s que a campanha
   perseguia era sobre *recusa*, não capacidade.

E daí o achado que reordenou tudo: **o default do produto é
`clamp(núcleos físicos, 4, 32)` — neste host, 4 lanes. A campanha rodava com 2**,
abaixo do piso do próprio default, e o raciocínio dela (§3.4 do pré-registro)
está medido como errado.

**A oitava emenda (§4.8) parou de sobrepor o default.** Com `lanes = 0` a escada
pré-final passou inteira, zero recusa de carga em 80 ciclos — sem uma linha de
código mudar.

**O mecanismo, se você precisar explicar a alguém:** uma lane bloqueada num
handler **não consome CPU**; ela segura um slot de concorrência. Dimensionar
lanes por núcleo trata lane como worker ligado a CPU, e ela não é.

## 3. O que fazer quando o Final 1 terminar

1. **Ler o veredito**: `grep "^###" ~/final1.log` no host. Ele já imprime
   `analisador=`, `recusas_de_carga=` e `handler_capacity=`.
2. **Puxar o artefato antes do Final 2** — o disco não comporta os dois inteiros
   com folga. O padrão usado nesta sessão:
   ```bash
   ssh ... 'cd ~/ladder4 && tar cz --exclude="cycles/*" --exclude="telemetry/stats-*" .' > l.tgz
   ```
   e commitar em `evidence/2026-08-05-r2-wp04-default-ladder/steps/final1/`.
3. **Se `recusas_de_carga` > 0:** o C22 vale como escrito. O WP04 para de novo, e
   aí o caminho volta a ser o **B3** — tirar a `/health` das lanes de aplicação —
   agora com o argumento muito mais forte, porque nem o default do produto
   bastou.
4. **Final 2 só em OUTRO DIA** (§9 do pré-registro). Não é burocracia: *"uma
   corrida boa sozinha não distingue 'estável' de 'teve uma boa noite'"*. Rodar
   hoje produz artefato inadmissível.
5. **Re-qualificar antes do Final 2**: `preflight.sh` + `smoke.sh`.
6. O script está pronto no host: `~/finais.sh` (edite o nome do run para
   `final2`).

## 4. O host de campanha

| | |
|---|---|
| endereço | `98.92.141.100` / `i-0dc43a340e9ab0388`, c5.2xlarge, us-east-1a |
| acesso | `ssh -i ~/Downloads/colossus.pem ubuntu@…` |
| identidade | Ubuntu 24.04.4, kernel `6.17.0-1017-aws`, Xeon 8275CL |
| qualificação | `preflight=pass`, vale até **2026-08-11** |
| disco | **24 GB total, ~18 livres** — é ele que limita a taxa (§4.8) |

**Dois hosts anteriores morreram em 2026-08-04, ambos sob carga de campanha.**
Sintomas diferentes (um travou o `sshd`, outro sumiu da rede) e **sem mecanismo
conhecido** — `conntrack`, `TIME_WAIT` e portas efêmeras foram descartados por
medição. Este terceiro sobreviveu com 60 s de folga entre braços, mas isso é
n = 1. **Se cair, o diagnóstico vem antes de relançar**: `dmesg`, contadores do
kernel, `nstat`.

**A receita de preparo de um host novo** está em `ESTADO.md` §7 — e a linha que
mais importa é desligar o auto-upgrade, porque foi um kernel trocado sozinho que
matou o primeiro host.

## 5. As regras que governam tudo, e que eu vi punirem quem as ignora

- **G1** — mudar código, toolchain **ou configuração load-bearing** cria
  candidato novo. A escada reinicia. Taxa e lanes são load-bearing.
- **G2** — todo instrumento prova a si mesmo: controle positivo **e mutante**. Um
  controle nunca visto vermelho é um controle não testado.
- **G3** — critérios congelados **antes** do run. Emenda depois de ver o
  resultado invalida o run.
- **C21/C24** — o `manifest.txt` tem de bater com o plano: as seis taxas e o
  `lanes = 0`. Foi essa checagem que pegou um harness rodando na taxa errada.

## 6. Achados abertos, nenhum bloqueante

| | O quê | Estado |
|---|---|---|
| **wp123** | `build/check.sh` reprovou uma vez com **507** (`ingest.begin` não abriu o spool); não reproduziu em 41 tentativas | aberto, sem causa |
| **ingest-leak** | `server did not start`; **não é lentidão** — 48 medições, pior caso 174 ms contra janela de 600 ms, com e sem upload | aberto, sem causa |
| **hosts caindo** | dois em um dia sob carga; três suspeitos eliminados | aberto, sem mecanismo |
| **F8** | limite de memória sob corpo hostil, pinado por argumento | a asserção pertence a um final |

Os dois primeiros envolvem `ingest` e podem ser o mesmo defeito. **Não os
"conserte" alargando janela ou serializando suíte** — os dois consertos óbvios
escondem a pergunta, e foi recusar isso que produziu a refutação útil.

## 7. O que continua sendo do dono

1. **Crescer o volume do host para 60 GB** — destravaria a taxa de f = 0,15
   (2.369/s). Hoje a campanha roda a **1.118/s porque o disco escolheu**, não o
   produto. Custa centavos e está declarado na §4.8.
2. **O teto de custo da Fase 4 do R3** — adiado até as Fases 0–3 fecharem
   (ADR-052).
3. **Re-ratificar a ambição** se o gatilho de abandono disparar.

## 8. O que fazer se você tiver tempo e o host estiver ocupado

Trabalho local que não toca a campanha, em ordem de valor:

1. **Montar o esqueleto do R2-WP08** — o pacote de decisão precisa de dez itens
   (`R2-restricted-production.md` §9) e nove deles já existem espalhados. Só os
   vereditos dos finais faltam.
2. **Atualizar o `ESTADO.md`** — a regra do próprio arquivo manda, e ele está
   descrevendo o WP04 como parado quando ele voltou a rodar.
3. **Investigar `wp123` + `ingest-leak`** — instrumentar `ingest.begin` para
   distinguir "abriu e falhou" de "não abriu", e ver se o slot de
   `max_concurrent` volta. É a pergunta que decide entre teste e produto.

**Não faça:** mexer em `web/` ou `ingest/` antes do WP08. Sob G1 isso cria
candidato novo e joga fora os finais que estão custando 24 h.

## 9. Os documentos que valem ler, nesta ordem

1. `planning/readiness/ESTADO.md` — o mapa
2. `planning/readiness/DECISOES-2026-08-05.md` — a ambição e o caminho
3. `ops/soak/campaigns/2026-08-02-r2-soak-candidate-1.md` §4.8 — a emenda vigente
4. `evidence/2026-08-05-r2-wp04-default-ladder/` — a escada que passou
5. `planning/r2-wp04-ladder-notes-2026-08-04.md` — os erros e as armadilhas
