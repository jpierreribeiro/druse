campaign=own-marshal walk, A/B and paired profiles
date_utc=2026-07-30
commit=528ae6a
host=AWS c5.2xlarge, 8 vCPU, Intel Xeon Platinum 8124M @ 3.00GHz
kernel=Linux 6.17.0-1017-aws x86_64
compiler=odin version dev-2026-07-nightly:819fdc7
endpoint=/json/medium/int
response_bytes=4310
lanes=4
server_cpus=0-3
load_cpus=4-7
report=docs/reports/2026-07-30-own-marshal.md

# The two binaries differ by ONE define over one commit; the A/B harness records
# these hashes itself and refuses to measure variants that answer different bytes.
sha256_control=a2b8fde9afa87c8acf7ef01ff5ecbdada6716d579e47b8de2df47cf7f83404f9
sha256_ownmarshal=21876449c8e9532eb16e32d23281ddb1dfca205d962224f9c76764826ff92643

# Three A/B campaigns. The first two are PRESERVED AND UNUSABLE as ceiling
# measurements: the own-marshal arm served 100% of the offered rate, so its
# goodput column reports the generator's setting, not the server's capacity.
# They are kept because the report explains why they were discarded.
ab_30k=censored; ownmarshal served 29,999.6/30,000 at p50 156us -> below its knee
ab_45k=censored; ownmarshal served 44,999/45,000 at p50 188us -> below its knee
ab_70k=valid; control 38.2% served at p50 8.1s, ownmarshal 92-94% at p50 336-478ms

# Paired profiles, both below the knee (served_share=100.0 in each manifest).
# The control was RE-PROFILED today: this morning's encode profile predates the
# type gate's adoption and would credit this change with that gain.
profile_control_samples=18198
profile_ownmarshal_samples=6089
cpu_time_ratio=0.3346 (control spends 2.99x the CPU for the same 10,000 req/s)

# WHAT WAS DROPPED BEFORE COMMITTING, and why it is not needed to recompute any
# published number: the openload binaries (7.5 MB each, rebuilt by the harness),
# the per-request load.csv streams (34 MB each), perf.data and perf-script.txt.
# Every figure in the report comes from summary.txt, the per-run JSON, and the
# Overhead column of perf-self.txt, all of which are kept. Re-running is the
# reproduction path, not re-reading a 34 MB stream.
dropped=openload, server, load.csv, perf.data, perf-script.txt, runs/*.csv
